import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'I Track: Mobile',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
